#!/usr/bin/env bash

# start telegram
if [[ ! `pidof telegram-desktop` ]]; then
    bspc rule -a "TelegramDesktop" -o desktop='^8'
    /usr/bin/telegram-desktop -startintray &
fi

# start polkit
if [[ ! `pidof xfce-polkit` ]]; then
	/usr/lib/xfce-polkit/xfce-polkit &
fi
